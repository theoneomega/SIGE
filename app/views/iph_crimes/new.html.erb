<%content_for :breadcrumb do%>
  <ul class="breadcrumb">
    <li><%=link_to t(:home), root_path %><span class="divider">/</span></li>
    <%if can? :index, Iph Crime %>
      <li><%= link_to t(:index), iph_crimes_path %><span class="divider">/</span></li>
    <%end%>
    <li class="active"><%=t(:new) + " " + t(:wiph_crime)%></li>
  </ul>
<%end%>
<%= render 'form' %>
