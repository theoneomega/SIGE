<%content_for :breadcrumb do%>
  <ul class="breadcrumb">
    <li><%=link_to t(:home) , root_path%><span class="divider">/</span></li>
    <%if can? :index, IphPerson %>
      <li><%= link_to t(:index), iph_people_path %><span class="divider">/</span></li>
    <%end%>
    <li class="active"><%=link_to t(:show), @iph_person %></li>
  </ul>
<%end%>
<%= render 'form' %>
