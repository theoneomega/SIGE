<%content_for :breadcrumb do%>
  <ul class="breadcrumb">
    <li><%=link_to t(:home) , root_path%><span class="divider">/</span></li>
    <%if can? :index, IphVehicle %>
      <li><%= link_to t(:index), iph_vehicles_path %><span class="divider">/</span></li>
    <%end%>
    <li class="active"><%=link_to t(:show), @iph_vehicle %></li>
  </ul>
<%end%>
<%= render 'form' %>
