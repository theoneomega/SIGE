<%content_for :breadcrumb do%>
  <ul class="breadcrumb">
    <li><%=link_to t(:home), root_path %><span class="divider">/</span></li>
    <%if can? :index, JusticeNet %>
      <li><%= link_to t(:index), justice_nets_path %><span class="divider">/</span></li>
    <%end%>
    <li class="active"><%=t(:new) + " " + t(:wjustice_net)%></li>
  </ul>
<%end%>
<%= render 'form' %>
