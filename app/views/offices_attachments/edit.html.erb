<%content_for :breadcrumb do%>
  <ul class="breadcrumb">
    <li><%=link_to t(:home) , root_path%><span class="divider">/</span></li>
    <%if can? :index, OfficesAttachment %>
      <li><%= link_to t(:index), offices_attachments_path %><span class="divider">/</span></li>
    <%end%>
    <li class="active"><%=link_to t(:show), @offices_attachment %></li>
  </ul>
<%end%>
<%= render 'form' %>
