

<div class="well">
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Id</th>
        <th>Role</th>
        <th>Protected</th>
        <th>Super admin</th>
        <th>Analyst</th>
        <th>IPH</th>
        <th>IPH Sueprvisor</th>
        <th>Administrator</th>
        <th>Supervisor</th>
        <th>Medios</th>
        <th>IE Supervisor</th>
        <th>Analysts Supervisor</th>
        <th>Super Supervisor</th>
        <td>Acciones</td>
      </tr>
    </thead>

    <% @roles.each do |role| %>
      <tr>
        <td><%= role.id %></td>
        <td><%= role.role %></td>
        <td><%= role.protected %></td>
        <td><%= role.super_admin %></td>
        <td><%= role.analyst %></td>
        <td><%= role.iph %></td>
        <td><%= role.iph_supervisor %></td>
        <td><%= role.administrator %></td>
        <td><%= role.supervisor %></td>
        <td><%= role.medios %></td>
        <td><%= role.ie_supervisor %></td>
        <td><%= role.analysts_supervisor %></td>
        <td><%= role.super_supervisor %></td>
        <td>
          <ul class="nav nav-pills">
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" title='<%=t(:actions)%>'><i class="icon-cog"></i>&nbsp;<i class="icon-caret-down"></i></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Show', role %></li>
                <li><%= link_to 'Edit', edit_role_path(role) %></li>
                <li><%= link_to 'Destroy', role, method: :delete, data: { confirm: 'Are you sure?' } %></li>
              </ul>
            </li>
          </ul>
        </td>
      </tr>
    <% end %>
  </table>
</div>
<br />

<%= link_to 'New Role', new_role_path %>
