<%content_for :breadcrumb do%>
  <ul class="breadcrumb">
    <li><%=link_to t(:home) , root_path%><span class="divider">/</span></li>
    <%if can? :index, Unit %>
      <li><%= link_to t(:index), units_path %><span class="divider">/</span></li>
    <%end%>
    <li class="active"><%=link_to t(:show), @unit %></li>
  </ul>
<%end%>
<%= render 'form' %>
