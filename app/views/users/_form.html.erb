<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label "Analista" %><br />
    <%= f.collection_select(:analyst_id, Analyst.order('id ASC').all, :id , :fullname) %>
  </div>
  <div class="field">
    <%= f.label "Rol" %><br />
    <%= f.collection_select(:role_id, Role.all, :id, :role) %>
  </div>

  <div class="field">
    <%= f.label "email" %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
